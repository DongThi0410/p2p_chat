PORT                 (Render tự set, không cần tạo)
DB_URL               (ví dụ: jdbc:postgresql://host:5432/chat_app)
DB_USER              (username DB)
DB_PASS              (password DB)
MAIL_USERNAME        (Gmail hoặc SMTP user)
MAIL_PASSWORD        (App password Gmail)
JWT_SECRET           (một chuỗi bí mật)
REDIS_HOST           (host Redis)
REDIS_PORT           (port Redis)
