<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Text?>

<StackPane xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
           fx:controller="org.example.peer_chat.ui.controllers.VoiceCallController"
           stylesheets="@../css/video-call-modal.css"
           styleClass="call-root">
    <BorderPane styleClass="call-background"
                mouseTransparent="true"/>
    <!-- Full-screen gradient background -->
    <BorderPane styleClass="call-background">

        <!-- Center content: avatar, name, timer, call type -->
        <center>
            <VBox alignment="CENTER" spacing="16">
                <Label fx:id="contactAvatar" text="🐰" styleClass="call-avatar" />

                <Label fx:id="contactName" text="Nguyễn Văn A" styleClass="call-name" />

                <HBox alignment="CENTER">
                    <Label fx:id="callDurationWrapper" text="">
                        <graphic>
                            <Label>
                                <graphic>
                                    <Text fx:id="callDuration" text="00:00" styleClass="call-duration-text" />
                                </graphic>
                            </Label>
                        </graphic>
                    </Label>
                </HBox>

                <HBox alignment="CENTER">
                    <Label fx:id="callTypePill" styleClass="call-type-pill">
                        <graphic>
                            <Text fx:id="callTypeLabel" text="Cuộc gọi thoại 🎤" />
                        </graphic>
                    </Label>
                </HBox>
            </VBox>
        </center>

<!--        &lt;!&ndash; Local video preview at top-right &ndash;&gt;-->
<!--        <top>-->
<!--            <HBox alignment="TOP_RIGHT" BorderPane.alignment="TOP_RIGHT">-->
<!--                <padding>-->
        <!--                    <Insets top="20" right="20" bottom="0" left="0" />-->
        <!--                </padding>-->
        <!--                <StackPane fx:id="localVideoContainer" styleClass="local-video-container">-->
        <!--                    <ImageView fx:id="localVideoFeed" fitWidth="220" fitHeight="150" />-->
        <!--                    <Label text="Camera của bạn 📷" styleClass="local-video-label" />-->
        <!--                </StackPane>-->
        <!--            </HBox>-->
        <!--        </top>-->

        <!-- Bottom controls -->
        <bottom>
            <HBox alignment="CENTER" spacing="18" styleClass="call-controls">
                <padding>
                    <Insets top="0" right="0" bottom="32" left="0"/>
                </padding>

                <!-- Accept / Reject buttons for incoming calls -->
                <Button fx:id="acceptCallButton" text="Chấp nhận" onAction="#onAccept"
                        styleClass="call-btn call-btn-accept" visible="false"/>
                <Button fx:id="rejectCallButton" text="Từ chối" onAction="#rejectIncomingCall"
                        styleClass="call-btn call-btn-reject" visible="false"/>

                <!-- In-call controls (shown khi đang trong cuộc gọi) -->
                <Button fx:id="muteButton" text="🔈" onAction="#toggleMute" styleClass="call-btn call-btn-mute"/>
                <Button fx:id="endCallButton" onAction="#endCall" text="✖" styleClass="call-btn call-btn-end"/>
            </HBox>
        </bottom>

    </BorderPane>
</StackPane>
